# Журнал разработки

Команда: KILC
Задача: 07. Образование: AI Sana Challenge
Соревновательная часть: 23.09.2026, 13:00–18:00 (время Астаны).

Журнал ведётся в соответствии с п. 5.4.8 Положения: по итогам каждого
отчётного часа фиксируется подтверждённый промежуточный результат.

---

## Час 1 (13:00 – 14:00)

- Сделано:
  - Правила работы: соглашение о коммитах, журнал, AGENTS.md, шаблон
    постановки задач для AI-агента, уточнённая модель данных.
  - go.mod с единственной зависимостью — `modernc.org/sqlite`.
  - Функция рейтинга `rating.Score`: 7 компонентов (20/20/15/15/10/10/10),
    разбивка по компонентам, подсказки «что добавить» с приростом баллов,
    уровни draft/working/ready/priority вычисляются из балла.
  - Endpoint `POST /api/rating` (пакет `api`) — расчёт рейтинга карточки
    без обращения к базе, с тестами на httptest.
- Артефакты (файлы, коммиты):
  - `COMMITS.md`, `PROGRESS.md`, `AGENTS.md`, `TASK_EXAMPLE.md`
    (48afe10, 0deb558, 00afefe, bd816d7)
  - `go.mod`, `go.sum` (da9cf07)
  - `rating/` (b54cc77)
  - `api/rating.go`, `api/rating_test.go` (feea062, a8c6715)
- Участники и вклад:
  - Жандос Кадыркулов — правила репозитория, рейтинг, API рейтинга.

## Час 2 (14:00 – 15:00)

- Сделано:
  - Каркас сервера: `main.go`, `GET /health`, схема SQLite (`schema.sql`)
    с `CHECK` на категорию, статус задачи и статус предложения;
    схема применяется при старте.
  - `store/`: CRUD задач с пересчётом `score` при каждом сохранении,
    публикация, каталог с сортировкой по рейтингу и фильтрами
    (категория, отрасль, уровень), список отраслей, справочник категорий.
  - `store/`: команды и предложения; валидация дедлайна и ссылки на
    прототип; принятие/отклонение только явным вызовом `DecideProposal`;
    подтверждение этапа `ConfirmStage` только для принятого предложения.
  - UI конструктора бизнес-задачи (`static/task-builder/`): форма,
    живой пересчёт рейтинга, черновик в localStorage, экспорт JSON.
- Артефакты (файлы, коммиты):
  - `main.go`, `schema.sql`, `store/store.go` (60b0a71)
  - `store/tasks.go`, `store/categories.go`, `store/errors.go` + тесты (0cbd491)
  - `store/teams.go`, `store/proposals.go` + тесты (89f9a23)
  - `static/task-builder/` (3875fb4)
  - Слияния: 2a63798 (`rating/contract`), c11cb80 (`platform/skeleton`)
- Участники и вклад:
  - Жандос Кадыркулов — сервер, схема, слой хранения, слияния веток.
  - urapke — конструктор бизнес-задачи (HTML/CSS/JS).
- Состояние на 14:25:
  - `go test ./api/... ./store/...` проходит.
  - `go test ./rating/...` падает: `TestScoreHintGainOrder` ожидает вторую
    подсказку по `users` (+10), а сортировка по приросту ставит
    `expected_result` (+15) — ошибка в ожидании теста.
- Осталось сделать:
  - Починить `TestScoreHintGainOrder`.
  - Подключить маршруты в `main.go`: сейчас зарегистрирован только
    `/health`; `api.Rating` не подключён, статика не встроена через `embed.FS`
    и не раздаётся.
  - Модуль `ai/`: интерфейс с двумя вызовами (черновик → ≥3 уточняющих
    вопроса; черновик + ответы → карточка), заглушка при `STUB=1`,
    фильтрация ключей ответа модели, откат на заглушку при ошибке.
    Ветка `origin/ai` пока без новых коммитов.
  - HTTP-обработчики: задачи (создание, редактирование, подтверждение
    полей, публикация), каталог с фильтрами, команды, предложения,
    решение по предложению, подтверждение этапа, баллы команды.
  - Баллы команды `SUM(points_awarded)` — запроса в `store/` ещё нет.
  - Сид-данные `seed.sql` / `data/`: 5 черновиков, 5 карточек, 5 команд,
    5 предложений; загрузка при пустой базе.
  - UI: конструктор считает рейтинг в JS по своим правилам
    (компонент «всё или ничего», одна галочка подтверждения на всю форму)
    и не сохраняет на сервер — перевести на `POST /api/rating` и
    сохранение задачи с подтверждением по полям; выбор категории из
    фиксированного списка.
  - Страницы: каталог, карточка задачи с формой предложения, экран
    бизнеса со списком предложений и кнопками принять/отклонить/
    подтвердить этап, переключатель роли (бизнес / команда).
  - `README.md`: инструкция запуска из чистого клона.

## Час 3 (15:00 – 16:00)

- Сделано:
- Артефакты (файлы, коммиты):
- Участники и вклад:

## Час 4 (16:00 – 17:00)

- Сделано:
- Артефакты (файлы, коммиты):
- Участники и вклад:

## Час 5 (17:00 – 18:00)

- Сделано:
- Артефакты (файлы, коммиты):
- Участники и вклад:
- Проверка запуска из чистого клона:
  - кто выполнял:
  - результат:
  - что исправлено по итогам:
