# Журнал разработки

Команда: KILC
Задача: 07. Образование: AI Sana Challenge
Соревновательная часть: 23.09.2026, 13:00–18:00 (время Астаны).

Журнал ведётся в соответствии с п. 5.4.8 Положения: по итогам каждого
отчётного часа фиксируется подтверждённый промежуточный результат.

---

## Час 1 (13:00 – 14:00)

- Сделано:
  - Правила работы: соглашение о коммитах, журнал, AGENTS.md, шаблон
    постановки задач для AI-агента, уточнённая модель данных.
  - go.mod с единственной зависимостью — `modernc.org/sqlite`.
  - Функция рейтинга `rating.Score`: 7 компонентов (20/20/15/15/10/10/10),
    разбивка по компонентам, подсказки «что добавить» с приростом баллов,
    уровни draft/working/ready/priority вычисляются из балла.
  - Endpoint `POST /api/rating` (пакет `api`) — расчёт рейтинга карточки
    без обращения к базе, с тестами на httptest.
- Артефакты (файлы, коммиты):
  - `COMMITS.md`, `PROGRESS.md`, `AGENTS.md`, `TASK_EXAMPLE.md`
    (48afe10, 0deb558, 00afefe, bd816d7)
  - `go.mod`, `go.sum` (da9cf07)
  - `rating/` (b54cc77)
  - `api/rating.go`, `api/rating_test.go` (feea062, a8c6715)
- Участники и вклад:
  - Жандос Кадыркулов — правила репозитория, рейтинг, API рейтинга.

## Час 2 (14:00 – 15:00)

- Сделано:
  - Каркас сервера: `main.go`, `GET /health`, схема SQLite (`schema.sql`)
    с `CHECK` на категорию, статус задачи и статус предложения;
    схема применяется при старте.
  - `store/`: CRUD задач с пересчётом `score` при каждом сохранении,
    публикация, каталог с сортировкой по рейтингу и фильтрами
    (категория, отрасль, уровень), список отраслей, справочник категорий.
  - `store/`: команды и предложения; валидация дедлайна и ссылки на
    прототип; принятие/отклонение только явным вызовом `DecideProposal`;
    подтверждение этапа `ConfirmStage` только для принятого предложения.
  - UI конструктора бизнес-задачи (`static/task-builder/`): форма,
    живой пересчёт рейтинга, черновик в localStorage, экспорт JSON.
- Артефакты (файлы, коммиты):
  - `main.go`, `schema.sql`, `store/store.go` (60b0a71)
  - `store/tasks.go`, `store/categories.go`, `store/errors.go` + тесты (0cbd491)
  - `store/teams.go`, `store/proposals.go` + тесты (89f9a23)
  - `static/task-builder/` (3875fb4)
  - Слияния: 2a63798 (`rating/contract`), c11cb80 (`platform/skeleton`)
- Участники и вклад:
  - Жандос Кадыркулов — сервер, схема, слой хранения, слияния веток.
  - urapke — конструктор бизнес-задачи (HTML/CSS/JS).
- Состояние на 14:25:
  - `go test ./api/... ./store/...` проходит.
  - `go test ./rating/...` падает: `TestScoreHintGainOrder` ожидает вторую
    подсказку по `users` (+10), а сортировка по приросту ставит
    `expected_result` (+15) — ошибка в ожидании теста.
- Осталось сделать на 14:25 (исторический снимок; актуальный список ниже):
  - Починить `TestScoreHintGainOrder`.
  - Подключить маршруты в `main.go`: сейчас зарегистрирован только
    `/health`; `api.Rating` не подключён, статика не встроена через `embed.FS`
    и не раздаётся.
  - Модуль `ai/`: интерфейс с двумя вызовами (черновик → ≥3 уточняющих
    вопроса; черновик + ответы → карточка), заглушка при `STUB=1`,
    фильтрация ключей ответа модели, откат на заглушку при ошибке.
    Ветка `origin/ai` пока без новых коммитов.
  - HTTP-обработчики: задачи (создание, редактирование, подтверждение
    полей, публикация), каталог с фильтрами, команды, предложения,
    решение по предложению, подтверждение этапа, баллы команды.
  - Баллы команды `SUM(points_awarded)` — запроса в `store/` ещё нет.
  - Сид-данные `seed.sql` / `data/`: 5 черновиков, 5 карточек, 5 команд,
    5 предложений; загрузка при пустой базе.
  - UI: конструктор считает рейтинг в JS по своим правилам
    (компонент «всё или ничего», одна галочка подтверждения на всю форму)
    и не сохраняет на сервер — перевести на `POST /api/rating` и
    сохранение задачи с подтверждением по полям; выбор категории из
    фиксированного списка.
  - Страницы: каталог, карточка задачи с формой предложения, экран
    бизнеса со списком предложений и кнопками принять/отклонить/
    подтвердить этап, переключатель роли (бизнес / команда).
  - `README.md`: инструкция запуска из чистого клона.

## Час 3 (15:00 – 16:00)

- Сделано:
  - Завершены работы конца второго часа: исправлено ожидание порядка
    подсказок рейтинга; подключены API рейтинга, конструктор и команды
    к Go/SQLite; каталог и статические ресурсы встроены через `embed.FS`.
  - Подключён seed при пустой базе: 5 черновиков, 5 опубликованных задач,
    5 команд и 5 предложений из `data/*.json`.
  - Каталог опубликованных задач с фильтрами категории, отрасли и уровня,
    сортировкой по `score DESC`; низкий рейтинг не скрывает карточку.
  - `ai/`: интерфейс двух вызовов, заглушка по умолчанию без API-ключа,
    клиент модели, проверка и очистка ответа, fallback с логированием.
    Пакет пока не подключён к HTTP-обработчикам и конструктору.
  - Рекомендации по интересам выбранной команды: до 5 опубликованных
    задач с рейтингом от 40; основной каталог сохраняет все уровни.
  - API и страницы откликов и кабинета бизнеса: создание предложения,
    ручное принятие/отклонение, подтверждение этапа принятого предложения.
    Баллы команды вычисляются через `SUM(points_awarded)`.
- Артефакты (файлы, коммиты):
  - Завершение часа 2: seed (5b79038), каталог (225bdbe), исправление
    теста рейтинга (5ac911c), маршрут рейтинга (ad55fbc), UI/API (12eeb41).
  - `ai/` (82eab6b, aae7c48, 8543719; слияние 46a4cd8).
  - Фильтры и рекомендации (2fded4d, 4048bca; слияние b0dedbc).
  - `api/proposals.go`, `api/business.go`, `store/business.go`, тесты,
    страницы задачи и бизнеса (51d3730); объединение API (bdb2468).
  - Обновление оформления страниц (438e0ad).
- Участники и вклад:
  - Жандос Кадыркулов — seed, рейтинг, каталог, AI, рекомендации, слияния.
  - urapke — подключение UI к API/SQLite, отклики, кабинет бизнеса,
    оформление страниц и объединение API.
  - sciddles — исходные шаблоны каталога (ee91252, конец часа 2).

## Час 4 (16:00 – 17:00)

- Сделано:
  - Общая навигация и выбор роли на страницах приложения.
  - В рейтинг добавлены отдельные `bonus` и `position` для вознаграждения;
    основной балл полноты остаётся 0–100. В конструкторе добавлены выбор
    типа, описание и подтверждение вознаграждения, показ бонуса и позиции.
  - Подключён справочник `GET /api/reward-types`.
    Сохранение вознаграждения и его использование каталогом пока
    не завершены в текущем дереве (см. список ниже).
- Артефакты (файлы, коммиты):
  - Общая навигация (a79cb0c; слияние 11eb11b).
  - Расчёт и тесты бонуса (303da5e, 5746294), работа над каталогом
    (8341e4b), элементы конструктора и API справочника (1a87530).
  - Текущий проверенный HEAD: 8f9880a, слияние `feat/reward`.
- Участники и вклад:
  - Жандос Кадыркулов — навигация, вознаграждения, рейтинг и слияния.

### Текущее состояние на 16:27 (Астана), 23.09.2026

Сверено с текущим кодом и историей коммитов, а не только с названиями
коммитов. Час 4 ещё не завершён.

- Проверка:
  - `go test ./rating/...` проходит.
  - `go test ./...` проходит для `ai`, `api`, `rating`, `store`.
  - `go run .` с временной SQLite-базой: первичный seed выполнен,
    повторный запуск корректно пропускает seed. Порт 8080 при запуске
    оказался занят; отдельный проверочный сервер запущен с `PORT=18080`.
    `curl -fsS localhost:18080/health` возвращает `{"status":"ok"}`.
    После освобождения порта в 16:27 сервер запущен обычным `go run .`
    с рабочей `data.db`; `curl -fsS localhost:8080/health` возвращает
    `{"status":"ok"}`.
- Осталось сделать / проверить перед сдачей:
  - Подключить два вызова `ai.Client` к серверу и конструктору:
    исходный текст → вопросы → ответы → карточка → ручное подтверждение.
  - Согласовать контракт вознаграждения: JS отправляет `reward_type`,
    `reward` и подтверждение `reward`, но `api.taskBody`, `store.Task`
    и схема не содержат полей вознаграждения. API отклоняет неизвестные
    поля; сквозное сохранение из обновлённого конструктора требует исправления.
    Каталог и рекомендации сейчас используют `score`, а не `position`.
  - Проверить серверные гарантии подтверждения: `UpdateTask` принимает
    переданный список `confirmed` без сравнения старых и новых значений;
    снятие подтверждения при редактировании сейчас реализовано в JS.
    Публикация проверяет 9 полей рейтинга, но не подтверждение категории
    и заголовка, которые может сформировать AI.
  - Пройти в браузере полный сценарий после исправлений: создание,
    редактирование, подтверждение, публикация, отклик, решение бизнеса,
    подтверждение этапа и баллы команды. Зелёные unit/API-тесты не
    подтверждают работоспособность всего пользовательского сценария.
  - Дополнить корневой `README.md` инструкцией запуска, настройками
    заглушки/модели и проверкой из чистого клона; актуализировать README
    конструктора после завершения интеграций.

## Час 5 (17:00 – 18:00)

- Сделано:
- Артефакты (файлы, коммиты):
- Участники и вклад:
- Проверка запуска из чистого клона:
  - кто выполнял:
  - результат:
  - что исправлено по итогам:
